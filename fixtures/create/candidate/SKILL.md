---
name: line-sorter
description: Sort newline-delimited text deterministically while preserving an empty input. Use when a task explicitly requires stable lexical line ordering.
---

# Line Sorter

Sort non-empty input lines with Unicode code-point order and emit one line per value. Preserve an empty input as empty output. Do not apply this Skill to explanatory questions about text formats.
